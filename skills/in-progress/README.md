# In-progress skills

Work in progress. **Not loaded** by the plugin — this category is intentionally
left out of `.claude-plugin/plugin.json`.

When a skill here is ready, move its folder into a live category
(`engineering`, `productivity`, `misc`, or `personal`). That's all it takes to
make it load.
